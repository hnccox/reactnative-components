import * as React from 'react';
import { View } from 'react-native';

type Props = {
	}

class InputButton extends React.Component<any, any> {
	constructor(props: Props) {
		super(props);
	}

	render(): React.ReactNode {
			return (
				<View>Button</View>
			)
	}
}

export default InputButton;