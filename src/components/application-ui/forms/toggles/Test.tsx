import React, {useState} from 'react';
import {
	Dimensions,
	useWindowDimensions,
  Text,
	View,
  TouchableOpacity,
} from 'react-native';
import tw from 'twrnc';

const TestComponent = () => {
	return (
		<Text>TESTING</Text>
	)
}

export default TestComponent